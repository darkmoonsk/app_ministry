import classes from "./HomeBody.module.css";

function HomeBody() {
  return (

	<section className={classes.container}>
		<div className={classes.news}>

		</div>
		{/* <img alt="Imagem representando relatorios" src={reportImage} /> */}
	</section>

  )
}

export default HomeBody;